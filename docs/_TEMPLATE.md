<!--
docs/*.md の共通テンプレート。各領域ドキュメントはこの構成に従うこと。
- 表は GitHub-flavored Markdown。
- 「全フィールド表」は reference を兼ねるので、調査で判明した全フィールドを省略せず載せる。
- 変換テーブルは mappings/<domain>.yaml と1:1で対応させる（同じ id を併記推奨）。
- 損失記号: ◎ lossless / ○ 形式変換のみ / △ lossy・降格 / ✕ dropped
- 末尾に必ず一次情報の出典 URL を列挙。
-->

# <領域名>: Claude Code ⇄ Codex

> 1〜2行サマリ（この領域の概念対応と最大の差分）。

## 0. 概要

両者でこの領域がどう扱われるか。概念が 1:1 か、片方にしか無いか。相互変換の難易度の総括。

## 1. Claude Code 側の仕様

### 配置・ファイル・スコープ
（ファイルパス、スコープ、優先順位）

### 全フィールド表
| フィールド | 型 | 必須 | デフォルト | スコープ | 説明 |
|---|---|---|---|---|---|

## 2. Codex 側の仕様

### 配置・ファイル・スコープ

### 全フィールド表
| フィールド | 型 | 必須 | デフォルト | スコープ | 説明 |
|---|---|---|---|---|---|

## 3. 変換テーブル

`mappings/<domain>.yaml` の人間可読版。

| id | Claude | Codex | 方向 | 損失 | 降格/スコープ | 書式変換・注記 |
|---|---|---|---|---|---|---|

## 4. 変換時の注意・既知の落とし穴

- スコープ降格、単位/極性変換、インデックスずれ、破棄項目、警告すべき点など。
- 既知バグ・仕様の流動性。

## 5. 出典

- 一次情報の URL を列挙。
